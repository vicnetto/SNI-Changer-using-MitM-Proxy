{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "lldb - Debug",
            "preLaunchTask": "build project",
            "program": "${workspaceFolder}/ssl-tls-proxy",
            "cwd": "${workspaceFolder}"
        }
    ]
}
